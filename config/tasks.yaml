# Vision tasks per camera. "type" must be registered in
# tasks/registry.py — the built-ins (auto-registered when the "tasks"
# package is imported, see tasks/__init__.py) are:
#   item_counting, ppe_compliance, missing_product, face_id
#
# The line/zone coordinates below are illustrative examples (assuming a
# ~1920x1080 frame) — use the Calibration screen of either interface to
# draw them over the live video instead of typing pixels by hand here.

cameras:
  cam1:
    tasks: []
      #- type: item_counting
      #  detect_fps: 5
      #  params:
      #    counting_line: {p1: [960, 0], p2: [960, 1080]}
      #    direction: any
      #    target_classes: [person]
      #    min_count_per_window: 1
      #    window_seconds: 120
      #  flags:
      #    - id: count_threshold
      #      enabled: true
      #      severity: warning
      #      notify: [log]

      # Face recognition — enroll employees through the "Employees" tab
      # before enabling this in production, otherwise every face becomes
      # an "unknown_face".
      # - type: face_id
      # detect_fps: 1
      # params:
      #   match_threshold: 0.45
      #   log_unknown: true
      # flags:
      #   - id: unknown_face
      #     enabled: true
      #     severity: info
      #     notify: [log]

  cam2:
    tasks: []
