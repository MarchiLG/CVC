"""
triggers

Condition -> action rules that fire on Flags (notify/flag.py) and drive
external IO devices — see triggers/engine.py (TriggerEngine) and
triggers/actions/ (the pluggable protocol backends).
"""
